<!DOCTYPE html>
<html>

    <head>
        <title> Community Connect - Timesheet </title>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <link rel="stylesheet"
    href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
    integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf"
    crossorigin="anonymous"/>
    </head>

    <body>

      <section class="container">

            <table class="">
              <tr>
                <th>Organization:</th>
                <th>Date:</th>
                <th>Hours:</th>
                <th>Signature:</th>
                <th></th>
              </tr>
              <% @opportunity.each do |opportunity| %>
                  <tr>
                      <td><%= opportunity.organization%></td>
                      <td><%= opportunity.date%></td>
                      <td><%= opportunity.hours%></td>
                      <td><%= opportunity.signature%></td>
                      <td><%= button_to "Delete", opportunity,:data => {:confirm => 'Are you sure?'}, method: :delete,class: "" %></td>
                  </tr>
              <%end%>
            </table>
          <%= button_to "ADD OPPORTUNITY", 'opportunities/new', method: :get ,class: "" %>
          <%= button_to "OPPORTUNITY LIST", 'services/', method: :get,class: ""  %>
          <%= button_to "Log out", '/session', method: :delete, class: "" %>


      </section>

</body>
</html>
